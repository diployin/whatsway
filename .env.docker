# Docker Environment Configuration
NODE_ENV=production
PORT=5000

# Database
DATABASE_URL=postgresql://whatsway:SecurePass123!@postgres:5432/whatsway_db
PGHOST=postgres
PGPORT=5432
PGDATABASE=whatsway_db
PGUSER=whatsway
PGPASSWORD=SecurePass123!
DB_SSL_MODE=disable

# Security
SESSION_SECRET=whatsway-docker-session-secret-change-in-production
ENCRYPTION_KEY=whatsway-docker-encryption-key-change-in-production

# WhatsApp API
WHATSAPP_API_VERSION=v21.0
WHATSAPP_WEBHOOK_VERIFY_TOKEN=your-webhook-verify-token

# Server
BASE_URL=http://localhost:5000
REPLIT_DOMAINS=localhost